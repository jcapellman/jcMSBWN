<Page
    x:Class="jcMSBWN.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:jcMSBWN.UWP"
    RequestedTheme="Dark"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" /> 
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Margin="10,10,10,10" Text="Select the networks you wish to be notified about when in range" />
        
        <ListBox Grid.Row="1" ItemsSource="{Binding Networks}" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Ssid}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Save" Icon="Save" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>